#!/bin/sh
# Scripts in the pre-init.d directory are run before any of the init scripts are run.
# This can be useful if you need to make changes to the container environment before the init scripts are run.